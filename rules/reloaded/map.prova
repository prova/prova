:- solve(test_map(X)).

data({'a':1,'b':2}).

test_map(X) :-
	data(X).
test_map(X) :-
	data({'a':1,'b':2}).
% Match the supplied variable. Note that a is the same as 'a' in Prova.
test_map(X) :-
	data({a:X,'b':2}).
