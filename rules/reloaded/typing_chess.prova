p(0):-eval(owl_typing("file:rules/ont/testOntology.rdf")).

isChesspiece(X^^ns:chesspiece).
isRook(X^^ns:rook).

:-solve(p(X)).
% should be X=1,2,3

p(1):-isChesspiece(X^^ns:pawn),println([X]).
p(2):-isRook(X^^ns:chesspiece).
p(3):-@forall(X,"ns:rook") isRook(X).
p(4):-isRook(X^^ns:pawn).

% Causes an exception
%:-solve(p(N,X)).
%p(1,X):-isChesspiece(X:ns:pawn).%,println([X]).
