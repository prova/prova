% This test can only be run from ProvaServiceTest.java

:- eval(receiver()).

receiver() :-
	println(["==========Service messaging test: receiver=========="]),

	rcvMsg(XID,Protocol,From,inform,a(I)),
	println(["Received ",a(I)]),
	$Count.incrementAndGet().
